services:
  api:
    extends:
      file: docker-compose.yaml
      service: api
    environment:
      - VLLM_CHAT_BASE_URL=${VLLM_CHAT_BASE_URL:-http://vllm-qwen35-35b-a3b-fp8:8000/v1}
      - VLLM_CHAT_MODEL=${VLLM_CHAT_MODEL:-Qwen3.5-35B-A3B-FP8}
      - VLLM_EMBED_BASE_URL=${VLLM_EMBED_BASE_URL:-http://litellm-gateway:8010/v1/embeddings}
      - VLLM_EMBED_MODEL=${VLLM_EMBED_MODEL:-Qwen3-Embedding-0.6B}
      - VLLM_RERANK_BASE_URL=${VLLM_RERANK_BASE_URL:-http://litellm-gateway:8010/v1/rerank}
      - VLLM_RERANK_MODEL=${VLLM_RERANK_MODEL:-bge-reranker-v2-m3}
      - LANG=C.UTF-8
      - LC_ALL=C.UTF-8
      - PYTHONUTF8=1
    command:
      - /bin/sh
      - -c
      - >
        WORKERS=$(( $(nproc) / 2 )); if [ "$$WORKERS" -lt 1 ]; then WORKERS=1; fi;
        exec uvicorn server.main:app --host 0.0.0.0 --port 5050 --workers "$$WORKERS" --loop uvloop --http httptools

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  crawler:
    extends:
      file: docker-compose.yaml
      service: crawler

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  crawler-worker:
    extends:
      file: docker-compose.yaml
      service: crawler-worker

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  app-redis:
    extends:
      file: docker-compose.yaml
      service: app-redis

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  redis-exporter:
    extends:
      file: docker-compose.yaml
      service: redis-exporter

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  web:
    extends:
      file: docker-compose.yaml
      service: web

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  graph:
    extends:
      file: docker-compose.yaml
      service: graph

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  etcd:
    extends:
      file: docker-compose.yaml
      service: etcd

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  minio:
    extends:
      file: docker-compose.yaml
      service: minio

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  milvus:
    extends:
      file: docker-compose.yaml
      service: milvus

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  postgres:
    extends:
      file: docker-compose.yaml
      service: postgres

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  postgres-exporter:
    extends:
      file: docker-compose.yaml
      service: postgres-exporter

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  prometheus:
    extends:
      file: docker-compose.yaml
      service: prometheus
    volumes:
      - ./docker/prometheus/prometheus.l20.qwen35.yml:/etc/prometheus/prometheus.yml:ro
      - ./docker/volumes/prometheus:/prometheus

    shm_size: 8g
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
  litellm-gateway:
    build:
      context: .
      dockerfile: docker/litellm.Dockerfile
      args:
        PYTHON_BASE_IMAGE: ${PYTHON_BASE_IMAGE:-docker.m.daocloud.io/python:3.11-slim}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.tuna.tsinghua.edu.cn/simple}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-pypi.tuna.tsinghua.edu.cn}
    image: ${LITELLM_IMAGE:-litellm-proxy:cn}
    container_name: litellm-gateway
    command:
      - --config
      - /app/config.yaml
      - --port
      - "8010"
    environment:
      - REDIS_HOST=${LITELLM_REDIS_HOST:-app-redis}
      - REDIS_PORT=${LITELLM_REDIS_PORT:-6379}
      - REDIS_PASSWORD=${LITELLM_REDIS_PASSWORD:-}
      - REDIS_DB=${LITELLM_REDIS_DB:-1}
    volumes:
      - ./docker/litellm.qwen35.yaml:/app/config.yaml:ro
    ports:
      - "8010:8010"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8010/health/readiness')"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 20s
    depends_on:
      - vllm-qwen35-35b-a3b-fp8
      - vllm-qwen3-embedding-cpu
      - vllm-bge-reranker-v2-m3-cpu
      - app-redis
    networks:
      - app-network
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
    shm_size: 8g
    ipc: host
    restart: unless-stopped

  vllm-qwen35-35b-a3b-fp8:
    image: ${VLLM_OPENAI_IMAGE:-docker.m.daocloud.io/vllm/vllm-openai:latest}
    container_name: vllm-qwen35-35b-a3b-fp8
    command:
      - --model
      - /models/Qwen/Qwen3.5-35B-A3B-FP8
      - --served-model-name
      - Qwen3.5-35B-A3B-FP8
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --tensor-parallel-size
      - "2"
      - --dtype
      - auto
      - --gpu-memory-utilization
      - "0.96"
      - --max-model-len
      - "16384"
      - --max-num-seqs
      - "40"
      - --max-num-batched-tokens
      - "32768"
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_coder
      - --trust-remote-code
    volumes:
      - ${MODEL_DIR:-./models}:/models
      - ./docker/volumes/vllm/hf-cache:/root/.cache/huggingface
    ports:
      - "8000:8000"
    ipc: host
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0", "1"]
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 20
      start_period: 300s
    networks:
      - app-network
    shm_size: 8g
    restart: unless-stopped

  vllm-qwen3-embedding-cpu:
    image: ${VLLM_OPENAI_CPU_IMAGE:-docker.m.daocloud.io/vllm/vllm-openai-cpu:latest}
    container_name: vllm-qwen3-embedding-cpu
    command:
      - --model
      - /models/Qwen/Qwen3-Embedding-0.6B
      - --runner
      - pooling
      - --served-model-name
      - Qwen3-Embedding-0.6B
      - --host
      - 0.0.0.0
      - --port
      - "8001"
      - --max-model-len
      - "1024"
      - --enforce-eager
      - --trust-remote-code
    environment:
      - CUDA_VISIBLE_DEVICES=
      - VLLM_CPU_OMP_THREADS_BIND=nobind
      - VLLM_CPU_KVCACHE_SPACE=1
    volumes:
      - ${MODEL_DIR:-./models}:/models
      - ./docker/volumes/vllm/hf-cache:/root/.cache/huggingface
    ports:
      - "8001:8001"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8001/health || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 20
      start_period: 120s
    networks:
      - app-network
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
    shm_size: 8g
    ipc: host
    restart: unless-stopped

  vllm-bge-reranker-v2-m3-cpu:
    image: ${VLLM_OPENAI_CPU_IMAGE:-docker.m.daocloud.io/vllm/vllm-openai-cpu:latest}
    container_name: vllm-bge-reranker-v2-m3-cpu
    command:
      - --model
      - /models/BAAI/bge-reranker-v2-m3
      - --served-model-name
      - bge-reranker-v2-m3
      - --host
      - 0.0.0.0
      - --port
      - "8002"
      - --max-model-len
      - "1024"
      - --enforce-eager
      - --trust-remote-code
    environment:
      - CUDA_VISIBLE_DEVICES=
      - VLLM_CPU_OMP_THREADS_BIND=nobind
      - VLLM_CPU_KVCACHE_SPACE=1
    volumes:
      - ${MODEL_DIR:-./models}:/models
      - ./docker/volumes/vllm/hf-cache:/root/.cache/huggingface
    ports:
      - "8002:8002"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8002/health || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 20
      start_period: 120s
    networks:
      - app-network
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
    shm_size: 8g
    ipc: host
    restart: unless-stopped

  paddlex:
    build:
      context: .
      dockerfile: docker/paddlex.Dockerfile
    image: paddlex:latest
    container_name: paddlex-ocr
    volumes:
      - ./docker/volumes/paddlex:/paddlex
      - ${MODEL_DIR:-./models}/PaddleX:/root/.paddlex
    ports:
      - "8080:8080"
    environment:
      - CUDA_VISIBLE_DEVICES=
    command:
      - paddlex
      - --serve
      - --pipeline
      - PP-StructureV3.yaml
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --device
      - cpu
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 8
      start_period: 90s
    networks:
      - app-network
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
      memlock:
        soft: -1
        hard: -1
    shm_size: 8g
    ipc: host
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
